

## 基于 React 不同版本的 todolist

```javascript
//通过不同版本的todolist学习，了解react的特性及数据管理方案
注意：因是不同时期的写法，样式与命名可能不一致

第一个版本：通过组件进行数据传递

第二个版本：使用hook + 组件数据传递 + localStorage本地存储

第三个版本：使用redux原生API的版本

第四个版本：使用hook + redux原生API的版本

第五个版本：使用react-redux的版本

第六个版本：使用react-redux + redux-thunk的版本

第七个版本：使用react-redux + redux-sage的版本

第八个版本：使用mobx的版本
```

